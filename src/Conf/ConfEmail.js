const ConfEmail = {
    serviceId : String(import.meta.env.VITE_EMAIL_SERVICE_ID),
    templateId : String(import.meta.env.VITE_EMAIL_TEMPLATE_ID),
    publicKey : String(import.meta.env.VITE_EMAIL_PUBLIC_KEY),
    
}

export default ConfEmail